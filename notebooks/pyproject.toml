[project]
name = "notebooks"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fiml",
    "marimo>=0.24.0",
    "pandas>=3.0.3",
    "pyarrow>=25.0.0",
]

[tool.uv.sources]
fiml = { path = "../crates/fiml-python", editable = true }
